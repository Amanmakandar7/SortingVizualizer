export const heapSort = async (array, setArray, speed, setIssorting, sleep) => {

    const arr = [...array]
    let n = arr.length;

    const heapify = (arr, n, i) =>{
        const largest = i;
        const left = 2*i+1;
        const right = 2*i+2;
        

    }

}